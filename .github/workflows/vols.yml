name: Vols
on:
  push:
    branches:
    - vols
    paths-ignore:
    - '**/README.md'
jobs:
  build:
    name: vols branch
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        shell: bash
        run: make
